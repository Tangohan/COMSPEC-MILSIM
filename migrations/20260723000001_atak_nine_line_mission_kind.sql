-- 9-Line : distinguer appui aérien (CAS) et évacuation sanitaire (MEDEVAC).
-- Préférer bootstrap/atak_nine_line_mission_kind_migration.php (idempotent).
SET NAMES utf8mb4;

-- ALTER exécuté via bootstrap si la colonne est absente.
